# Next.js and its SWC binaries resolve more reliably with a flat-ish tree.
# Keep this narrow rather than disabling strict peer resolution wholesale.
public-hoist-pattern[]=*next*
public-hoist-pattern[]=*tailwind*
